import toPicker from './src/element';
// import pwd from './test';

document.addEventListener('DOMContentLoaded', () => {
  if (process) {
    const pwd = process.cwd(); //
    // '/'  => './index.css'
    //  '/node_modules/datepicker' => './node_modules/datepicker/index.css'
    const cssPath = `.${pwd}index.css`;
    // const linkcss = document.createElement('LINK');
    // linkcss.setAttribute('rel', 'stylesheet');
    // linkcss.setAttribute('href', cssPath);
    // document.querySelector('head').appendChild(linkcss);
  }
  const input = document.querySelector('#input');
  toPicker(input);
});

export default toPicker;
